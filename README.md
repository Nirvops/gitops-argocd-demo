# gitops-argocd-demo
A sample app deployed to a local Kubernetes cluster (kind/k3d) via Argo CD, with GitHub Actions building and pushing the image on every commit and Argo CD auto-syncing the manifest change.
